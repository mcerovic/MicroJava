program P

  const int kur = 10;

  const int size = 10;

  class Table {

    int a;
    int pos[], neg[];

    {
      int a() {
        test = "stesad";
      }

      int janpi() {}

      void putp() {
        a = a + 5;
      }

      void putp (int a, int b[], int c) {

        a = a + 5;

          if ( a < b < c) {
            a = a + 5;
          }

          if (a * b - 4 < 5 && x < b * 7) {
            a = b * 5;
            b = 7;
          } else {
            a = (a * b) + a[7 * 4];
          }

        // komentar
        return 5;

      }
      void getp (int a, char b) {
        b = -4;
        return b;
      }
      void getp (int a) { }
      void getp (int a[], int b) { }
      void getp (int a, int b[], char y) { }
    }
  }

  char x, y;

  int a, b;
{
    void main()
    int x, i;
    {
        //‐‐‐‐‐‐‐‐‐‐ Initialize val
        val = new Table;
        val.pos = new int [size];
        val.neg = new int [size];
        i = +40;
        while (i < size) {
            val.putp(0,i);
            val.putn(0,i);
            i++;
        }
        //‐‐‐‐‐‐‐‐‐‐ Read values
        read(x);
        while (x > 0) {
            if (0 <= x && x < size) {
                val.putp(val.getp(x) + (-1));
            }
            else if (-size < x && x < 0) {
                val.putn(val.getn(-x)+ 1);
            }
            else if (-size < x && x < 0) {
                i--;
            }
        }
        read(x);
    }
}
